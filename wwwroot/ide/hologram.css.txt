.hologram-page {
    min-height: 100vh;
    padding-top: 78px;
    background: #020203;
}

.holo-stage {
    --holo-orange: #ff9b4a;
    --holo-bright: #ffd8ab;
    --holo-line: rgba(255, 155, 74, 0.18);

    position: relative;
    min-height: calc(100vh - 78px);
    overflow: hidden;
    background:
        radial-gradient(circle at 50% 38%, rgba(255, 149, 61, 0.1), transparent 28%),
        linear-gradient(180deg, #050405 0%, #020203 100%);
    perspective: 2400px;
    touch-action: none;
    user-select: none;
    cursor: grab;
}

.holo-stage:active {
    cursor: grabbing;
}

.holo-stage-clean::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at 50% 70%, rgba(255, 120, 25, 0.1), transparent 24%),
        radial-gradient(circle at 18% 20%, rgba(255, 155, 74, 0.04), transparent 18%),
        radial-gradient(circle at 82% 18%, rgba(255, 155, 74, 0.04), transparent 18%);
}

.holo-grid-overlay {
    position: absolute;
    inset: 0;
    background:
        linear-gradient(rgba(255, 155, 74, 0.045) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 155, 74, 0.045) 1px, transparent 1px);
    background-size: 26px 26px;
    mask-image: radial-gradient(circle at center, black 46%, transparent 86%);
    opacity: 0.42;
}

.holo-ring {
    position: absolute;
    left: 50%;
    top: 72%;
    border-radius: 50%;
    border: 1px solid rgba(255, 155, 74, 0.16);
    transform: translate(-50%, -50%) rotateX(78deg);
    box-shadow: 0 0 26px rgba(255, 145, 54, 0.1);
    animation: holoPulse 7s linear infinite;
}

.ring-one {
    width: 420px;
    height: 150px;
}

.ring-two {
    width: 620px;
    height: 210px;
    animation-delay: -2.4s;
    opacity: 0.78;
}

.ring-three {
    width: 860px;
    height: 290px;
    animation-delay: -4.7s;
    opacity: 0.46;
}

.holo-floor {
    position: absolute;
    left: 50%;
    bottom: 58px;
    width: min(1120px, 90vw);
    height: 240px;
    transform: translateX(-50%) rotateX(80deg);
    border-radius: 50%;
    background:
        radial-gradient(circle, rgba(255, 205, 154, 0.55) 0 1px, transparent 1.8px) center / 16px 16px,
        radial-gradient(circle at center, rgba(255, 126, 26, 0.16), transparent 68%);
    border: 1px solid rgba(255, 155, 74, 0.14);
    box-shadow: 0 0 42px rgba(255, 118, 12, 0.1);
}

.holo-glow-core {
    position: absolute;
    left: 50%;
    top: 54%;
    width: 520px;
    height: 520px;
    transform: translate(-50%, -50%);
    background: radial-gradient(circle, rgba(255, 165, 94, 0.18), transparent 72%);
    filter: blur(24px);
}

.holo-rig {
    --holo-rx: -8deg;
    --holo-ry: -42deg;
    --holo-scale: 1.02;

    position: absolute;
    left: 50%;
    top: 54%;
    width: 1040px;
    height: 760px;
    transform-style: preserve-3d;
    transform: translate(-50%, -50%) scale(var(--holo-scale)) rotateX(var(--holo-rx)) rotateY(var(--holo-ry));
    will-change: transform;
}

.holo-computer {
    position: relative;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
}

.holo-box {
    position: absolute;
    width: var(--w);
    height: var(--h);
    transform-style: preserve-3d;
}

.holo-face {
    position: absolute;
    border: 1px solid rgba(255, 155, 74, 0.28);
    background:
        radial-gradient(circle, rgba(255, 210, 162, 0.94) 0 1px, transparent 1.8px) center / 11px 11px,
        linear-gradient(180deg, rgba(255, 170, 98, 0.18), rgba(255, 104, 26, 0.04));
    box-shadow:
        inset 0 0 22px rgba(255, 184, 119, 0.06),
        0 0 18px rgba(255, 143, 54, 0.08);
}

.holo-front,
.holo-back {
    width: 100%;
    height: 100%;
}

.holo-front {
    transform: translateZ(calc(var(--d) / 2));
}

.holo-back {
    transform: rotateY(180deg) translateZ(calc(var(--d) / 2));
    opacity: 0.42;
}

.holo-left,
.holo-right {
    width: var(--d);
    height: 100%;
    left: calc(50% - (var(--d) / 2));
    opacity: 0.56;
}

.holo-left {
    transform: rotateY(-90deg) translateZ(calc(var(--w) / 2));
}

.holo-right {
    transform: rotateY(90deg) translateZ(calc(var(--w) / 2));
}

.holo-top,
.holo-bottom {
    width: 100%;
    height: var(--d);
    top: calc(50% - (var(--d) / 2));
    opacity: 0.58;
}

.holo-top {
    transform: rotateX(90deg) translateZ(calc(var(--h) / 2));
}

.holo-bottom {
    transform: rotateX(-90deg) translateZ(calc(var(--h) / 2));
}

.holo-desktop-box {
    --w: 560px;
    --h: 142px;
    --d: 320px;

    top: 428px;
    left: 240px;
    transform: translateZ(16px);
}

.holo-desktop-box .holo-face {
    background:
        radial-gradient(circle, rgba(255, 219, 176, 0.94) 0 1px, transparent 1.8px) center / 11px 11px,
        linear-gradient(180deg, rgba(255, 182, 113, 0.18), rgba(255, 111, 28, 0.06));
}

.holo-desktop-front {
    position: absolute;
    inset: 14px 16px 12px;
    display: grid;
    grid-template-columns: 1.15fr 1fr;
    gap: 16px;
    padding: 18px 20px 14px;
    border: 1px solid rgba(255, 177, 105, 0.22);
    background:
        radial-gradient(circle at 18% 18%, rgba(255, 222, 183, 0.22), transparent 24%),
        linear-gradient(180deg, rgba(255, 200, 151, 0.08), rgba(28, 10, 3, 0.78));
    box-shadow: inset 0 0 24px rgba(255, 163, 77, 0.08);
}

.holo-desktop-panel {
    position: relative;
    border: 1px solid rgba(255, 176, 103, 0.2);
    background:
        linear-gradient(180deg, rgba(255, 194, 141, 0.08), rgba(27, 9, 3, 0.72)),
        radial-gradient(circle at 20% 20%, rgba(255, 229, 196, 0.08), transparent 26%);
}

.holo-desktop-panel-left {
    border-radius: 6px;
}

.holo-panel-slot {
    position: absolute;
    left: 24px;
    top: 26px;
    height: 10px;
    border: 1px solid rgba(255, 189, 122, 0.26);
    background: linear-gradient(180deg, rgba(6, 2, 1, 0.88), rgba(38, 13, 4, 0.42));
    box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.5);
}

.holo-panel-slot-wide {
    right: 36px;
}

.holo-panel-latch {
    position: absolute;
    right: 20px;
    top: 21px;
    width: 20px;
    height: 18px;
    border-radius: 3px;
    border: 1px solid rgba(255, 185, 116, 0.24);
    background: radial-gradient(circle at 35% 35%, rgba(255, 233, 204, 0.58), rgba(255, 118, 36, 0.14));
}

.holo-panel-pin {
    position: absolute;
    left: 18px;
    top: 18px;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255, 232, 204, 0.84), rgba(255, 128, 38, 0.18));
}

.holo-desktop-panel-right {
    display: grid;
    grid-template-columns: 1fr 52px;
    gap: 12px;
    padding: 10px 12px 10px 14px;
}

.holo-drive-stack {
    display: grid;
    gap: 8px;
}

.holo-drive-bay {
    position: relative;
    border: 1px solid rgba(255, 179, 109, 0.2);
    background:
        linear-gradient(180deg, rgba(255, 191, 132, 0.1), rgba(23, 8, 3, 0.74)),
        radial-gradient(circle at 14% 18%, rgba(255, 233, 201, 0.08), transparent 24%);
}

.holo-drive-bay::after {
    content: "";
    position: absolute;
    left: 10px;
    right: 10px;
    top: 50%;
    height: 2px;
    transform: translateY(-50%);
    background: linear-gradient(90deg, rgba(255, 173, 101, 0.08), rgba(255, 213, 175, 0.76), rgba(255, 173, 101, 0.08));
}

.holo-drive-bay-large {
    min-height: 34px;
}

.holo-power-cluster {
    position: relative;
    border-left: 1px solid rgba(255, 175, 105, 0.18);
}

.holo-power-button {
    position: absolute;
    right: 8px;
    bottom: 12px;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    border: 1px solid rgba(255, 191, 125, 0.26);
    background:
        radial-gradient(circle at 35% 35%, rgba(255, 234, 207, 0.72), rgba(255, 131, 43, 0.18)),
        linear-gradient(180deg, rgba(255, 197, 142, 0.12), rgba(17, 5, 2, 0.74));
}

.holo-explorer-shell {
    display: grid;
    grid-template-columns: 76px minmax(0, 1fr);
    width: 100%;
    height: 100%;
    border: 1px solid rgba(160, 211, 255, 0.14);
    background:
        linear-gradient(180deg, rgba(8, 14, 22, 0.98), rgba(3, 6, 11, 0.99)),
        radial-gradient(circle at top left, rgba(97, 183, 255, 0.12), transparent 24%);
    overflow: hidden;
}

.holo-explorer-sidebar {
    padding: 7px 6px;
    border-right: 1px solid rgba(142, 211, 255, 0.14);
    background: linear-gradient(180deg, rgba(15, 26, 40, 0.96), rgba(8, 15, 24, 0.98));
    display: grid;
    align-content: start;
    gap: 5px;
}

.holo-explorer-heading {
    font-size: 0.5rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(196, 234, 255, 0.62);
    margin-bottom: 1px;
}

.holo-explorer-nav,
.holo-mini-button {
    border: 1px solid rgba(143, 210, 255, 0.16);
    background: rgba(73, 147, 255, 0.06);
    color: #e8f7ff;
    padding: 6px;
    font: inherit;
    font-size: 0.56rem;
    cursor: pointer;
    border-radius: 6px;
}

.holo-explorer-nav.active,
.holo-explorer-nav:hover,
.holo-mini-button:hover {
    background: rgba(97, 181, 255, 0.14);
    border-color: rgba(181, 228, 255, 0.3);
}

.holo-explorer-main {
    display: grid;
    grid-template-rows: auto 1fr;
    min-width: 0;
    position: relative;
}

.holo-explorer-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
    padding: 6px 8px;
    border-bottom: 1px solid rgba(161, 214, 255, 0.12);
    background: rgba(103, 187, 255, 0.05);
}

.holo-explorer-path {
    font-size: 0.56rem;
    color: rgba(216, 242, 255, 0.78);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.holo-explorer-body {
    display: block;
    min-height: 0;
    overflow: auto;
    padding: 0 2px 2px 0;
}

.holo-folder-grid {
    padding: 8px;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 6px;
    align-content: start;
    min-height: 100%;
}

.holo-folder-card {
    border: 1px solid rgba(147, 213, 255, 0.14);
    background:
        linear-gradient(180deg, rgba(81, 169, 255, 0.07), rgba(6, 11, 18, 0.92)),
        radial-gradient(circle at top left, rgba(213, 242, 255, 0.08), transparent 26%);
    color: #e9f7ff;
    padding: 7px 6px 8px;
    text-align: center;
    min-height: 60px;
    cursor: pointer;
    display: grid;
    justify-items: center;
    align-content: start;
    gap: 4px;
    border-radius: 8px;
}

.holo-folder-card:hover {
    border-color: rgba(182, 228, 255, 0.28);
    transform: translateY(-1px);
}

.holo-folder-badge {
    width: 24px;
    height: 20px;
    border-radius: 6px;
    border: 1px solid rgba(160, 216, 255, 0.18);
    background:
        linear-gradient(180deg, rgba(110, 198, 255, 0.2), rgba(16, 66, 104, 0.28)),
        radial-gradient(circle at top left, rgba(226, 248, 255, 0.2), transparent 34%);
    display: grid;
    place-items: center;
    font-size: 0.45rem;
    letter-spacing: 0.12em;
    color: rgba(228, 248, 255, 0.92);
}

.holo-folder-card.drive .holo-folder-badge {
    background:
        linear-gradient(180deg, rgba(131, 208, 255, 0.24), rgba(23, 80, 120, 0.26)),
        radial-gradient(circle at top left, rgba(224, 247, 255, 0.22), transparent 34%);
    color: #e8f7ff;
}

.holo-folder-card.folder .holo-folder-badge {
    background:
        linear-gradient(180deg, rgba(255, 193, 118, 0.28), rgba(133, 77, 13, 0.24)),
        radial-gradient(circle at top left, rgba(255, 241, 212, 0.22), transparent 34%);
    color: #fff2d8;
}

.holo-folder-title {
    font-size: 0.56rem;
    font-weight: 700;
    line-height: 1.22;
    word-break: break-word;
}

.holo-preview-panel {
    padding: 10px 11px;
    display: grid;
    align-content: start;
    gap: 8px;
    overflow: auto;
    background:
        linear-gradient(180deg, rgba(7, 12, 19, 0.86), rgba(4, 8, 14, 0.96)),
        radial-gradient(circle at top left, rgba(113, 196, 255, 0.06), transparent 30%);
}

.holo-preview-kicker {
    font-size: 0.6rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(205, 237, 255, 0.6);
}

.holo-preview-copy {
    font-size: 0.62rem;
    line-height: 1.52;
    color: #e7f7ff;
}

.holo-preview-copy p {
    margin: 0 0 8px;
}

.holo-preview-image {
    width: 100%;
    border-radius: 10px;
    border: 1px solid rgba(173, 224, 255, 0.16);
    background: rgba(255, 255, 255, 0.04);
}

.holo-file-window {
    position: absolute;
    inset: 34px 12px 12px;
    z-index: 4;
    display: grid;
    grid-template-rows: auto 1fr;
    border: 1px solid rgba(175, 225, 255, 0.18);
    border-radius: 10px;
    overflow: hidden;
    background:
        linear-gradient(180deg, rgba(10, 16, 24, 0.98), rgba(4, 8, 13, 0.98)),
        radial-gradient(circle at top left, rgba(140, 210, 255, 0.08), transparent 26%);
    box-shadow: 0 16px 30px rgba(0, 0, 0, 0.32);
    max-width: calc(100% - 24px);
    max-height: calc(100% - 46px);
}

.holo-file-window-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
    min-height: 26px;
    padding: 4px 7px;
    border-bottom: 1px solid rgba(161, 215, 255, 0.14);
    background: rgba(101, 184, 255, 0.07);
}

.holo-file-window-title {
    font-size: 0.54rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(230, 248, 255, 0.88);
}

.holo-file-window-content {
    min-height: 0;
    overflow: auto;
    padding: 9px 10px;
}

.holo-file-window-copy {
    font-size: 0.58rem;
    line-height: 1.5;
    color: #e7f7ff;
}

.holo-file-window-copy p {
    margin: 0 0 8px;
}

.holo-desktop-top-vent {
    position: absolute;
    inset: 26px 52px 44px 180px;
    border-radius: 10px;
    background:
        repeating-linear-gradient(90deg, rgba(255, 214, 177, 0.7) 0 2px, transparent 2px 8px),
        linear-gradient(180deg, rgba(255, 195, 138, 0.14), rgba(21, 7, 3, 0.72));
    opacity: 0.72;
}

.holo-monitor-pedestal {
    position: absolute;
    left: 430px;
    top: 346px;
    width: 180px;
    height: 116px;
    transform-style: preserve-3d;
    transform: translateZ(70px);
}

.holo-pedestal-disc,
.holo-pedestal-column {
    position: absolute;
    left: 50%;
    transform-style: preserve-3d;
}

.holo-pedestal-disc {
    border: 1px solid rgba(255, 182, 114, 0.22);
    background:
        radial-gradient(circle, rgba(255, 219, 176, 0.92) 0 1px, transparent 1.8px) center / 10px 10px,
        linear-gradient(180deg, rgba(255, 191, 129, 0.14), rgba(27, 10, 3, 0.78));
    box-shadow: inset 0 0 16px rgba(255, 175, 102, 0.08);
}

.holo-pedestal-disc-bottom {
    width: 170px;
    height: 54px;
    bottom: 0;
    margin-left: -85px;
    border-radius: 50%;
    transform: rotateX(78deg);
}

.holo-pedestal-column {
    width: 96px;
    height: 44px;
    bottom: 20px;
    margin-left: -48px;
    border: 1px solid rgba(255, 179, 112, 0.22);
    border-radius: 12px;
    background:
        radial-gradient(circle, rgba(255, 218, 173, 0.9) 0 1px, transparent 1.8px) center / 10px 10px,
        linear-gradient(180deg, rgba(255, 195, 136, 0.14), rgba(26, 9, 3, 0.76));
}

.holo-pedestal-disc-top {
    width: 120px;
    height: 38px;
    bottom: 34px;
    margin-left: -60px;
    border-radius: 50%;
    transform: rotateX(78deg);
}

.holo-monitor-box {
    --w: 350px;
    --h: 326px;
    --d: 366px;

    top: 88px;
    left: 342px;
    transform: translateZ(84px);
}

.holo-monitor-box .holo-face {
    background:
        radial-gradient(circle, rgba(255, 223, 183, 0.94) 0 1px, transparent 1.8px) center / 11px 11px,
        linear-gradient(180deg, rgba(255, 189, 121, 0.18), rgba(255, 110, 25, 0.06));
}

.holo-monitor-front {
    position: absolute;
    inset: 8px 9px 10px;
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 8px 9px 8px;
    border: 1px solid rgba(255, 179, 109, 0.22);
    background:
        radial-gradient(circle at 24% 18%, rgba(255, 226, 190, 0.18), transparent 24%),
        linear-gradient(180deg, rgba(255, 197, 142, 0.08), rgba(24, 8, 3, 0.8));
    box-shadow: inset 0 0 26px rgba(255, 171, 90, 0.08);
}

.holo-monitor-bezel {
    position: relative;
    flex: 1;
    padding: 6px 7px 9px;
    border: 1px solid rgba(255, 180, 111, 0.2);
    background:
        linear-gradient(180deg, rgba(255, 205, 157, 0.1), rgba(26, 9, 3, 0.82)),
        radial-gradient(circle at 22% 18%, rgba(255, 236, 211, 0.08), transparent 30%);
    clip-path: polygon(1.5% 0, 98.5% 0, 100% 100%, 0 100%);
}

.holo-screen-shell {
    position: relative;
    width: 100%;
    height: 100%;
    padding: 4px 5px 7px;
    border: 1px solid rgba(255, 176, 104, 0.2);
    background:
        linear-gradient(180deg, rgba(255, 205, 154, 0.11), rgba(38, 14, 4, 0.8)),
        radial-gradient(circle at 30% 22%, rgba(255, 211, 168, 0.14), transparent 34%);
    clip-path: polygon(2.5% 0, 97.5% 0, 100% 100%, 0 100%);
    box-shadow:
        inset 0 0 22px rgba(255, 178, 108, 0.08),
        0 0 20px rgba(255, 128, 32, 0.06);
}

.holo-screen-bevel {
    position: relative;
    width: 100%;
    height: 100%;
    padding: 3px 4px 6px;
    border: 1px solid rgba(255, 180, 110, 0.18);
    background:
        linear-gradient(180deg, rgba(255, 193, 132, 0.09), rgba(18, 6, 2, 0.88));
    clip-path: polygon(3.5% 0, 96.5% 0, 100% 100%, 0 100%);
    box-shadow: inset 0 0 20px rgba(255, 175, 102, 0.08);
}

.holo-screen {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    border-radius: 10px 10px 28px 28px / 10px 10px 36px 36px;
    border: 1px solid rgba(255, 170, 98, 0.24);
    background:
        radial-gradient(circle at 50% 28%, rgba(255, 177, 111, 0.14), transparent 44%),
        linear-gradient(180deg, rgba(255, 123, 43, 0.11), rgba(6, 2, 1, 0.94));
    box-shadow:
        inset 0 0 36px rgba(255, 188, 124, 0.08),
        inset 0 -30px 54px rgba(0, 0, 0, 0.44);
}

.holo-screen.desktop-active .holo-screen-terminal,
.holo-screen.desktop-active .holo-screen-sun,
.holo-screen.desktop-active .holo-screen-cloud,
.holo-screen.desktop-active .holo-screen-horizon,
.holo-screen.desktop-active .holo-screen-mountain,
.holo-screen.desktop-active .holo-screen-ground,
.holo-screen.desktop-active .holo-screen-water {
    opacity: 0.08;
}

.holo-screen.terminal-active .holo-screen-terminal,
.holo-screen.terminal-active .holo-screen-sun,
.holo-screen.terminal-active .holo-screen-cloud,
.holo-screen.terminal-active .holo-screen-horizon,
.holo-screen.terminal-active .holo-screen-mountain,
.holo-screen.terminal-active .holo-screen-ground,
.holo-screen.terminal-active .holo-screen-water,
.holo-screen.explorer-active .holo-screen-terminal,
.holo-screen.explorer-active .holo-screen-sun,
.holo-screen.explorer-active .holo-screen-cloud,
.holo-screen.explorer-active .holo-screen-horizon,
.holo-screen.explorer-active .holo-screen-mountain,
.holo-screen.explorer-active .holo-screen-ground,
.holo-screen.explorer-active .holo-screen-water {
    opacity: 0.16;
}

.holo-screen::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
        linear-gradient(180deg, rgba(255, 213, 165, 0.1), transparent 26%),
        radial-gradient(circle at 24% 18%, rgba(255, 225, 196, 0.14), transparent 20%);
    pointer-events: none;
}

.holo-screen::after {
    content: "";
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at 50% 110%, rgba(0, 0, 0, 0.5), transparent 44%),
        radial-gradient(circle at 50% -6%, rgba(255, 228, 191, 0.1), transparent 22%);
    pointer-events: none;
}

.holo-desktop-surface {
    position: absolute;
    inset: 2px;
    z-index: 3;
    display: grid;
    grid-template-rows: auto 1fr auto;
    gap: 7px;
    padding: 6px 8px 8px;
    background:
        linear-gradient(135deg, rgba(7, 14, 24, 0.98) 0%, rgba(11, 24, 42, 0.96) 36%, rgba(6, 14, 24, 0.98) 100%);
    color: #dff6ff;
}

.holo-desktop-surface::before,
.holo-desktop-surface::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
}

.holo-desktop-surface::before {
    background:
        radial-gradient(circle at 18% 14%, rgba(120, 208, 255, 0.2), transparent 26%),
        radial-gradient(circle at 78% 20%, rgba(34, 126, 255, 0.14), transparent 24%),
        linear-gradient(124deg, transparent 0 36%, rgba(104, 196, 255, 0.1) 42%, transparent 48%),
        linear-gradient(149deg, transparent 0 52%, rgba(255, 151, 61, 0.1) 58%, transparent 65%),
        linear-gradient(168deg, transparent 0 67%, rgba(50, 117, 219, 0.12) 72%, transparent 79%);
}

.holo-desktop-surface::after {
    background:
        radial-gradient(circle at 72% 68%, rgba(255, 136, 49, 0.12), transparent 20%),
        linear-gradient(180deg, rgba(255, 255, 255, 0.03), transparent 22%),
        repeating-linear-gradient(90deg, rgba(155, 220, 255, 0.04) 0 1px, transparent 1px 46px),
        repeating-linear-gradient(0deg, rgba(155, 220, 255, 0.025) 0 1px, transparent 1px 34px);
    mix-blend-mode: screen;
    opacity: 0.9;
}

.holo-desktop-topbar {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
    min-height: 24px;
    padding: 4px 8px;
    border-radius: 999px;
    border: 1px solid rgba(150, 220, 255, 0.12);
    background:
        linear-gradient(180deg, rgba(5, 10, 17, 0.92), rgba(2, 7, 13, 0.88)),
        radial-gradient(circle at top left, rgba(156, 224, 255, 0.08), transparent 30%);
}

.holo-desktop-status {
    display: flex;
    gap: 5px;
    align-items: center;
}

.holo-status-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(213, 245, 255, 0.96), rgba(119, 203, 255, 0.34));
    box-shadow: 0 0 8px rgba(111, 201, 255, 0.18);
}

.holo-desktop-time {
    color: rgba(225, 248, 255, 0.84);
    font-size: 0.58rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
}

.holo-desktop-icons {
    position: relative;
    z-index: 1;
    display: grid;
    align-content: start;
    justify-items: start;
    gap: 8px;
    padding: 2px 0 0 1px;
}

.holo-desktop-icon {
    width: 74px;
    min-height: 66px;
    border: 1px solid transparent;
    background: transparent;
    color: #eefbff;
    padding: 5px 4px;
    display: grid;
    justify-items: center;
    align-content: start;
    gap: 5px;
    cursor: pointer;
    font: inherit;
    text-align: center;
    border-radius: 10px;
}

.holo-desktop-icon:hover {
    border-color: rgba(149, 223, 255, 0.24);
    background: rgba(81, 180, 255, 0.08);
}

.holo-desktop-glyph {
    position: relative;
    width: 38px;
    height: 31px;
    border-radius: 8px;
    border: 1px solid rgba(156, 227, 255, 0.26);
    box-shadow:
        inset 0 0 10px rgba(183, 232, 255, 0.08),
        0 0 10px rgba(82, 188, 255, 0.08);
}

.holo-desktop-glyph-terminal {
    background:
        linear-gradient(180deg, rgba(43, 255, 112, 0.16), rgba(9, 57, 19, 0.42)),
        linear-gradient(135deg, rgba(14, 18, 24, 0.96), rgba(5, 7, 10, 0.98));
}

.holo-desktop-glyph-terminal::before {
    content: ">_";
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    color: #baffc6;
    font-size: 0.6rem;
    letter-spacing: 0.08em;
}

.holo-desktop-glyph-explorer {
    background:
        linear-gradient(180deg, rgba(123, 203, 255, 0.24), rgba(21, 73, 112, 0.34)),
        linear-gradient(135deg, rgba(11, 24, 38, 0.96), rgba(4, 10, 18, 0.98));
}

.holo-desktop-glyph-explorer::before {
    content: "";
    position: absolute;
    left: 4px;
    top: 5px;
    width: 15px;
    height: 6px;
    border-radius: 4px 4px 0 0;
    background: linear-gradient(180deg, rgba(220, 244, 255, 0.86), rgba(108, 197, 255, 0.24));
}

.holo-desktop-glyph-explorer::after {
    content: "";
    position: absolute;
    left: 4px;
    right: 4px;
    bottom: 5px;
    top: 10px;
    border-radius: 6px;
    border: 1px solid rgba(195, 234, 255, 0.2);
    background:
        linear-gradient(180deg, rgba(141, 215, 255, 0.2), rgba(25, 78, 115, 0.28)),
        radial-gradient(circle at top left, rgba(225, 248, 255, 0.2), transparent 36%);
}

.holo-desktop-glyph-drive {
    background:
        linear-gradient(180deg, rgba(255, 194, 118, 0.24), rgba(112, 65, 8, 0.34)),
        linear-gradient(135deg, rgba(42, 28, 8, 0.96), rgba(16, 10, 3, 0.98));
}

.holo-desktop-glyph-drive::before {
    content: "";
    position: absolute;
    left: 5px;
    top: 6px;
    width: 19px;
    height: 6px;
    border-radius: 4px 4px 0 0;
    background: linear-gradient(180deg, rgba(255, 233, 194, 0.72), rgba(255, 187, 107, 0.18));
}

.holo-desktop-glyph-drive::after {
    content: "";
    position: absolute;
    left: 5px;
    right: 5px;
    bottom: 5px;
    top: 11px;
    border-radius: 6px;
    border: 1px solid rgba(255, 220, 171, 0.18);
    background:
        linear-gradient(180deg, rgba(255, 212, 143, 0.16), rgba(121, 77, 12, 0.34)),
        radial-gradient(circle at top left, rgba(255, 244, 219, 0.18), transparent 34%);
}

.holo-desktop-label {
    max-width: 66px;
    font-size: 0.58rem;
    line-height: 1.3;
    text-shadow: 0 0 10px rgba(0, 0, 0, 0.36);
}

.holo-desktop-taskbar {
    position: relative;
    z-index: 1;
    display: flex;
    gap: 6px;
    align-items: center;
    min-height: 26px;
    padding: 4px 8px;
    border-radius: 999px;
    border: 1px solid rgba(145, 221, 255, 0.12);
    background:
        linear-gradient(180deg, rgba(4, 10, 18, 0.9), rgba(2, 7, 12, 0.88)),
        radial-gradient(circle at top left, rgba(128, 208, 255, 0.08), transparent 28%);
}

.holo-taskbar-chip {
    padding: 3px 7px;
    border-radius: 999px;
    background: rgba(86, 183, 255, 0.08);
    color: rgba(228, 248, 255, 0.84);
    font-size: 0.54rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.holo-screen-window {
    position: absolute;
    inset: 10px 12px 12px;
    z-index: 6;
    display: grid;
    grid-template-rows: auto 1fr;
    overflow: hidden;
    border: 1px solid rgba(155, 221, 255, 0.16);
    border-radius: 10px;
    background:
        linear-gradient(180deg, rgba(7, 13, 21, 0.96), rgba(4, 8, 14, 0.98)),
        radial-gradient(circle at top left, rgba(81, 183, 255, 0.08), transparent 28%);
    box-shadow: 0 0 22px rgba(0, 0, 0, 0.22);
}

.holo-screen-window-terminal {
    top: 6px;
    right: 6px;
    bottom: 6px;
    left: 6px;
    background:
        linear-gradient(180deg, rgba(2, 12, 4, 0.97), rgba(0, 5, 1, 0.99)),
        radial-gradient(circle at top left, rgba(68, 255, 111, 0.06), transparent 24%);
}

.holo-screen-window-explorer {
    top: 12px;
    right: 12px;
    bottom: 14px;
    left: 14px;
    background:
        linear-gradient(180deg, rgba(8, 14, 22, 0.98), rgba(3, 6, 11, 0.99)),
        radial-gradient(circle at top left, rgba(92, 186, 255, 0.08), transparent 28%);
}

.holo-screen-window-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
    min-height: 28px;
    padding: 5px 8px;
    border-bottom: 1px solid rgba(151, 220, 255, 0.12);
    background: rgba(128, 204, 255, 0.05);
}

.holo-screen-window-title {
    font-size: 0.58rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(229, 248, 255, 0.86);
}

.holo-screen-window-actions {
    display: flex;
    gap: 5px;
}

.holo-terminal-shell {
    display: grid;
    grid-template-rows: 1fr auto;
    min-height: 0;
    gap: 6px;
    padding: 8px 9px 9px;
    background:
        radial-gradient(circle at top left, rgba(101, 255, 124, 0.08), transparent 24%),
        linear-gradient(180deg, rgba(1, 12, 3, 0.9), rgba(1, 6, 2, 0.98));
}

.holo-terminal-output {
    position: relative;
    min-height: 0;
    overflow: auto;
    cursor: text;
    padding: 8px 9px 22px;
    border: 1px solid rgba(110, 255, 146, 0.14);
    border-radius: 8px;
    background:
        linear-gradient(180deg, rgba(6, 20, 8, 0.92), rgba(2, 8, 3, 0.98)),
        repeating-linear-gradient(180deg, rgba(130, 255, 158, 0.03) 0 1px, transparent 1px 4px);
    box-shadow: inset 0 0 18px rgba(44, 255, 88, 0.05);
}

.holo-terminal-text {
    margin: 0;
    white-space: pre-wrap;
    word-break: break-word;
    color: #c9ffcf;
    font-size: 0.58rem;
    line-height: 1.4;
    font-family: Consolas, "Courier New", monospace;
    text-shadow: 0 0 8px rgba(76, 255, 113, 0.08);
}

.holo-terminal-skip {
    position: absolute;
    right: 7px;
    bottom: 6px;
    color: rgba(198, 255, 204, 0.56);
    font-size: 0.5rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.holo-terminal-input-shell {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 6px;
    align-items: center;
    padding: 6px 8px;
    border: 1px solid rgba(110, 255, 146, 0.14);
    border-radius: 8px;
    background: rgba(4, 18, 7, 0.88);
}

.holo-terminal-prompt {
    color: #88ff9a;
    font-size: 0.58rem;
}

.holo-terminal-input {
    width: 100%;
    min-width: 0;
    border: none;
    outline: none;
    padding: 0;
    background: transparent;
    color: #eefff0;
    font: inherit;
    font-size: 0.58rem;
    font-family: Consolas, "Courier New", monospace;
    user-select: text;
    -webkit-user-select: text;
}

.holo-drag-indicator {
    position: absolute;
    left: 50%;
    bottom: 22px;
    z-index: 3;
    display: inline-flex;
    align-items: center;
    gap: 12px;
    transform: translateX(-50%);
    padding: 8px 12px;
    border: 1px solid rgba(255, 170, 92, 0.16);
    border-radius: 999px;
    background:
        linear-gradient(180deg, rgba(12, 6, 3, 0.74), rgba(5, 3, 2, 0.86)),
        radial-gradient(circle at top left, rgba(255, 200, 138, 0.06), transparent 28%);
    box-shadow: 0 0 22px rgba(255, 129, 27, 0.08);
    pointer-events: none;
}

.holo-drag-copy {
    color: rgba(255, 220, 182, 0.8);
    font-size: 0.62rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
}

.holo-drag-arrow {
    position: relative;
    width: 28px;
    height: 8px;
    opacity: 0.8;
}

.holo-drag-arrow::before,
.holo-drag-arrow::after {
    content: "";
    position: absolute;
    top: 50%;
    background: linear-gradient(90deg, rgba(255, 174, 101, 0.08), rgba(255, 222, 188, 0.88), rgba(255, 174, 101, 0.08));
}

.holo-drag-arrow::before {
    left: 0;
    right: 0;
    height: 1px;
    transform: translateY(-50%);
}

.holo-drag-arrow::after {
    width: 8px;
    height: 8px;
    border-top: 1px solid rgba(255, 223, 191, 0.88);
    border-right: 1px solid rgba(255, 223, 191, 0.88);
    background: none;
}

.holo-drag-arrow-left::after {
    left: 1px;
    transform: translateY(-50%) rotate(-135deg);
}

.holo-drag-arrow-right::after {
    right: 1px;
    transform: translateY(-50%) rotate(45deg);
}

.holo-terminal-input::placeholder {
    color: rgba(196, 255, 203, 0.34);
}

.holo-screen-terminal {
    position: absolute;
    top: 8%;
    left: 8%;
    width: 46%;
    display: grid;
    grid-template-columns: 18px 1fr;
    grid-template-rows: repeat(3, 6px);
    gap: 6px 10px;
    z-index: 2;
    filter: drop-shadow(0 0 8px rgba(255, 187, 124, 0.18));
}

.holo-term-icon {
    grid-row: 1 / span 3;
    width: 12px;
    height: 12px;
    margin-top: 1px;
    border-radius: 50%;
    border: 1px solid rgba(255, 204, 153, 0.5);
    background: radial-gradient(circle at 35% 35%, rgba(255, 236, 215, 0.86), rgba(255, 136, 48, 0.16));
}

.holo-term-line {
    align-self: center;
    width: 84%;
    height: 3px;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(255, 168, 94, 0.06), rgba(255, 214, 173, 0.94), rgba(255, 168, 94, 0.06));
}

.holo-term-line-main {
    width: 100%;
}

.holo-term-line-short {
    width: 60%;
}

.holo-screen-sun {
    position: absolute;
    left: 52%;
    top: 26%;
    width: 104px;
    height: 104px;
    transform: translateX(-50%);
    border-radius: 50%;
    background:
        radial-gradient(circle, rgba(255, 229, 192, 0.48), rgba(255, 182, 102, 0.22) 52%, rgba(255, 130, 46, 0.06) 71%, transparent 72%);
    box-shadow: 0 0 28px rgba(255, 179, 104, 0.12);
    opacity: 0.84;
}

.holo-screen-cloud {
    position: absolute;
    height: 8px;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(255, 178, 107, 0.08), rgba(255, 226, 194, 0.82), rgba(255, 178, 107, 0.08));
    opacity: 0.66;
}

.holo-screen-cloud::before,
.holo-screen-cloud::after {
    content: "";
    position: absolute;
    top: -4px;
    width: 22px;
    height: 14px;
    border-radius: 999px;
    background: inherit;
}

.holo-screen-cloud::before {
    left: 14px;
}

.holo-screen-cloud::after {
    right: 12px;
}

.holo-screen-cloud-top {
    top: 15%;
    right: 12%;
    width: 86px;
}

.holo-screen-cloud-mid {
    top: 37%;
    right: 8%;
    width: 62px;
    transform: scale(0.86);
}

.holo-screen-horizon {
    position: absolute;
    left: 0;
    right: 0;
    top: 44%;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(255, 216, 174, 0.94), transparent);
    opacity: 0.82;
}

.holo-screen-mountain {
    position: absolute;
    left: 18%;
    width: 64%;
    bottom: 15%;
    height: 58%;
    filter: drop-shadow(0 0 18px rgba(255, 148, 60, 0.08));
}

.holo-mountain-ridge {
    position: absolute;
    display: block;
}

.holo-mountain-ridge-main {
    inset: 0;
    clip-path: polygon(50% 0, 83% 100%, 17% 100%);
    background: linear-gradient(180deg, rgba(255, 234, 208, 0.88), rgba(255, 188, 118, 0.3) 58%, rgba(255, 112, 33, 0.08));
}

.holo-mountain-ridge-shadow {
    right: 13%;
    bottom: 0;
    width: 42%;
    height: 94%;
    clip-path: polygon(0 12%, 100% 100%, 18% 100%);
    background: linear-gradient(180deg, rgba(255, 192, 124, 0.34), rgba(255, 97, 27, 0.06));
}

.holo-mountain-ridge-base {
    left: 6%;
    right: 6%;
    bottom: 0;
    height: 35%;
    clip-path: polygon(0 78%, 14% 58%, 29% 72%, 43% 46%, 58% 70%, 76% 52%, 100% 80%, 100% 100%, 0 100%);
    background: linear-gradient(180deg, rgba(255, 221, 183, 0.52), rgba(255, 141, 52, 0.12));
}

.holo-screen-ground {
    position: absolute;
    bottom: 16%;
    width: 28%;
    height: 26%;
    background: linear-gradient(180deg, rgba(255, 182, 108, 0.2), rgba(255, 112, 34, 0.02));
}

.holo-screen-ground-left {
    left: -1%;
    clip-path: polygon(0 76%, 25% 52%, 44% 34%, 59% 52%, 69% 26%, 100% 100%, 0 100%);
}

.holo-screen-ground-right {
    right: -2%;
    clip-path: polygon(0 100%, 27% 32%, 45% 50%, 63% 26%, 80% 48%, 100% 72%, 100% 100%);
}

.holo-screen-water {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 6%;
    height: 17%;
    background:
        linear-gradient(180deg, transparent 0, rgba(255, 216, 181, 0.1) 25%, rgba(255, 157, 74, 0.18) 100%),
        repeating-linear-gradient(180deg, rgba(255, 214, 177, 0.72) 0 2px, transparent 2px 8px);
    opacity: 0.84;
}

.holo-screen-grid {
    position: absolute;
    inset: 0;
    background:
        linear-gradient(rgba(255, 178, 111, 0.12) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 178, 111, 0.06) 1px, transparent 1px);
    background-size: 100% 12px, 14px 100%;
    mix-blend-mode: screen;
    opacity: 0.42;
    pointer-events: none;
}

.holo-art-scan {
    position: absolute;
    left: 0;
    right: 0;
    top: -28%;
    height: 42%;
    background: linear-gradient(180deg, rgba(255, 196, 130, 0), rgba(255, 190, 122, 0.2), rgba(255, 196, 130, 0));
    animation: holoScan 4.2s linear infinite;
}

.holo-art-glow {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(255, 190, 122, 0.12), transparent 74%);
}

.holo-monitor-controls {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: 10px;
    min-height: 22px;
    padding-right: 10px;
}

.holo-monitor-led {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255, 232, 198, 0.86), rgba(255, 124, 38, 0.2));
}

.holo-monitor-power {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    border: 1px solid rgba(255, 190, 123, 0.24);
    background:
        radial-gradient(circle at 35% 35%, rgba(255, 234, 206, 0.76), rgba(255, 126, 38, 0.18)),
        linear-gradient(180deg, rgba(255, 197, 140, 0.1), rgba(21, 7, 3, 0.76));
}

.holo-monitor-side-vent,
.holo-monitor-top-vent {
    position: absolute;
    border-radius: 8px;
    background:
        repeating-linear-gradient(180deg, rgba(255, 213, 176, 0.76) 0 2px, transparent 2px 7px),
        linear-gradient(180deg, rgba(255, 194, 138, 0.12), rgba(20, 7, 3, 0.74));
    opacity: 0.74;
}

.holo-monitor-side-vent {
    inset: 74px 28px 80px 28px;
}

.holo-monitor-top-vent {
    inset: 24px 42px 36px 52px;
    background:
        repeating-linear-gradient(90deg, rgba(255, 213, 176, 0.76) 0 2px, transparent 2px 7px),
        linear-gradient(180deg, rgba(255, 194, 138, 0.12), rgba(20, 7, 3, 0.74));
}

@keyframes holoFloat {
    0%,
    100% {
        transform: translateY(0);
    }
    50% {
        transform: translateY(-10px);
    }
}

@keyframes holoPulse {
    0% {
        opacity: 0.28;
        transform: translate(-50%, -50%) rotateX(78deg) scale(0.97);
    }
    50% {
        opacity: 0.7;
        transform: translate(-50%, -50%) rotateX(78deg) scale(1.02);
    }
    100% {
        opacity: 0.28;
        transform: translate(-50%, -50%) rotateX(78deg) scale(0.97);
    }
}

@keyframes holoScan {
    from {
        top: -28%;
    }
    to {
        top: 100%;
    }
}

@media (max-width: 1180px) {
    .holo-rig {
        --holo-scale: 0.88;
    }
}

@media (max-width: 860px) {
    .holo-rig {
        --holo-scale: 0.72;
        top: 55%;
    }

    .holo-explorer-shell {
        grid-template-columns: 70px minmax(0, 1fr);
    }

    .holo-folder-grid {
        grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .holo-file-window {
        inset: 32px 10px 10px;
    }

    .holo-terminal-text,
    .holo-file-window-copy,
    .holo-folder-title,
    .holo-explorer-path,
    .holo-screen-window-title,
    .holo-file-window-title,
    .holo-terminal-prompt,
    .holo-terminal-input,
    .holo-mini-button,
    .holo-explorer-nav {
        font-size: 0.72rem;
    }
}

@media (max-width: 640px) {
    .hologram-page {
        padding-top: 72px;
    }

    .holo-stage {
        min-height: calc(100vh - 72px);
    }

    .holo-floor {
        width: 96vw;
        height: 168px;
        bottom: 34px;
    }

    .holo-rig {
        --holo-scale: 0.58;
        top: 54%;
    }

    .holo-drag-indicator {
        gap: 9px;
        padding: 7px 10px;
        bottom: 18px;
    }

    .holo-drag-copy {
        font-size: 0.54rem;
    }

    .holo-folder-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .holo-file-window {
        inset: 28px 8px 8px;
    }

    .holo-explorer-shell {
        grid-template-columns: 56px minmax(0, 1fr);
    }

    .holo-explorer-sidebar {
        padding: 6px 4px;
    }

    .holo-desktop-icon {
        width: 82px;
        min-height: 72px;
    }

    .holo-terminal-input-shell {
        padding: 8px 9px;
    }

    .holo-terminal-text,
    .holo-file-window-copy,
    .holo-folder-title,
    .holo-explorer-path,
    .holo-screen-window-title,
    .holo-file-window-title {
        font-size: 0.82rem;
    }

    .holo-terminal-prompt,
    .holo-terminal-input,
    .holo-mini-button,
    .holo-explorer-nav {
        font-size: 16px;
    }

    .holo-mini-button,
    .holo-explorer-nav {
        min-height: 32px;
    }
}
