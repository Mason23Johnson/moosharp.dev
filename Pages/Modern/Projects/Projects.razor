@page "/projects"
@layout MainLayout

<section class="projects-page">
    <header class="projects-hero">
        <p class="projects-kicker">MooSharp</p>
        <h1 class="projects-page-title">Projects</h1>
        <div class="projects-title-rule"></div>
    </header>

    <section class="projects-feature">
        <div class="feature-art">
            <img src="/images/projects/tinek.png" alt="Tinek" />
        </div>
        <div class="feature-copy">
            <p class="projects-kicker">Roadmap Lead</p>
            <h2>Tinek</h2>
            <p>
                Tinek is being built as a hunting prediction platform powered by AI models and mathematical logic.
                The plan is to combine smart forecasts, field data storage, scouting notes, and long-term utility in
                one system that works on both mobile and web.
            </p>
            <div class="feature-meta">
                <a class="chip chip-link" href="https://tinek.us" target="_blank" rel="noopener noreferrer">tinek.us</a>
                <span class="chip">Out Now</span>
                <span class="chip">Prediction Engine</span>
                <span class="chip">Data Storage</span>
                <span class="chip">Field Utility</span>
            </div>
        </div>
    </section>

    <section class="uploads-grid">
        @foreach (var item in Items)
        {
            <article class="upload-card">
                <div class="upload-card-shell">
                    <div class="upload-card-face upload-card-front">
                        <div class="upload-thumb">
                            @if (!string.IsNullOrEmpty(item.Thumbnail))
                            {
                                <img src="@item.Thumbnail" alt="@item.Title" />
                            }
                            else
                            {
                                <div class="upload-thumb-placeholder">
                                    <span>@item.Platform</span>
                                </div>
                            }
                            <span class="upload-status">@item.Status</span>
                        </div>
                        <div class="upload-body">
                            <div class="upload-heading">
                                <h4>@item.Title</h4>
                                <span class="upload-platform">@item.Platform</span>
                            </div>
                            <p>@item.Summary</p>
                            <div class="upload-tags">
                                @foreach (var tag in item.Tags)
                                {
                                    <span>@tag</span>
                                }
                            </div>
                        </div>
                    </div>

                    <div class="upload-card-face upload-card-back">
                        <div class="upload-hover-panel">
                            <p class="hover-kicker">Deeper Look</p>
                            <h5>@item.Title</h5>
                            <p>@item.DeepDescription</p>
                            <p class="hover-build">@item.BuildNotes</p>
                        </div>
                    </div>
                </div>

                <div class="upload-footer">
                    @if (!string.IsNullOrWhiteSpace(item.ProjectUrl))
                    {
                        @if (item.IsDownload)
                        {
                            <a class="upload-link" href="@item.ProjectUrl" download>Download</a>
                        }
                        else
                        {
                            <a class="upload-link" href="@item.ProjectUrl" target="@(item.IsExternal ? "_blank" : null)" rel="@(item.IsExternal ? "noopener noreferrer" : null)">Open</a>
                        }
                    }
                    else if (string.IsNullOrWhiteSpace(item.GithubUrl))
                    {
                        <span class="upload-link disabled">Coming Soon</span>
                    }
                    @if (!string.IsNullOrWhiteSpace(item.GithubUrl))
                    {
                        <a class="upload-link upload-link-ghost" href="@item.GithubUrl" target="_blank" rel="noopener noreferrer">GitHub</a>
                    }
                </div>
            </article>
        }
    </section>

</section>

@code {
    private readonly List<ProjectItem> Items = new()
    {
        new(
            "BeamRunners",
            "High-speed neon arena multiplayer - leave your trail, trap your rivals, last beam standing wins.",
            null,
            null,
            "Coming Soon",
            "Steam / PC",
            new[] { "Multiplayer", "Arena", "Steam" },
            "BeamRunners drops players into a neon arena where every move leaves a permanent glowing trail. The grid fills fast - cut off opponents, force bad angles, and be the last one running.",
            "Built for tight competitive sessions with fast matchmaking and a clean neon aesthetic. Targeting a Steam release.",
            IsDownload: false,
            IsExternal: false),
        new(
            "rParrot",
            "A screen reader with voice output for accessibility on Windows.",
            "/images/projects/rParrot.png",
            "/downloads/rParrot.exe",
            "Download",
            "Windows",
            new[] {"Accessibility", "Screen Reader" },
            "rParrot is a screen reader application that uses Windows Voice to provide voice output, making Windows applications more accessible for visually impaired users.",
            "Packaged as a standalone Windows executable - download and run, no installation required.",
            IsDownload: true,
            IsExternal: false,
            GithubUrl: "https://github.com/Mason23Johnson/rParrot"),
        new(
            "Windows Button Disabler",
            "Disable the Windows key and system shortcuts during gameplay to stop accidental alt-tabs.",
            "/images/projects/winDisable.png",
            null,
            "Open Source",
            "Windows",
            new[] { "Gaming", "Utility", "Windows" },
            "A lightweight utility that blocks the Windows key and other disruptive system shortcuts while gaming, preventing accidental minimization mid-match.",
            "Targeted initially at Overwatch but works across any fullscreen game on Windows.",
            IsDownload: false,
            IsExternal: false,
            GithubUrl: "https://github.com/Mason23Johnson/Video-Game-Windows-Button-Disabler-Currently-Overwatch-"),
        new(
            "MooCastScoreboard",
            "Livestream scoreboard editor for sports streams and quick game-day overlays.",
            "/images/projects/MooCast.png",
            "/MooCast",
            "Live",
            "Web",
            new[] { "Scoreboard", "Streaming", "Utility" },
            "MooCastScoreboard is meant to be a practical streamer utility: update scores fast, switch sports, adjust team styling, and keep the overlay readable under pressure.",
            "Built as a focused Blazor tool with sport-aware controls, editable team branding, and a live preview so streamers can tune the layout before it goes on-screen.",
            IsDownload: false,
            IsExternal: false),
        new(
            "Moochine Arcade",
            "A growing hub for prototypes, arcade experiments, and game concepts.",
            "/images/projects/MoochineArcade.png",
            "/arcade",
            "Playable",
            "Web",
            new[] { "Arcade", "Prototype Lab", "Games" },
            "Moochine Arcade is the testing ground for quick ideas, playable prototypes, and game concepts that can grow into bigger projects over time.",
            "Built with Blazor, JavaScript, lightweight game logic, and a storefront-style shell that makes the collection feel more intentional than a loose page of links.",
            IsDownload: false,
            IsExternal: false),
        new(
            "Arctis 7 HID Protocol",
            "Reverse engineering the USB HID protocol for SteelSeries Arctis 7 headsets.",
            null,
            null,
            "Research",
            "Hardware",
            new[] { "Reverse Engineering", "USB HID", },
            "A deep dive into the USB HID protocol used by the SteelSeries Arctis 7 - documenting byte structures of commands and building custom control software outside the official app.",
            "USB packet captures, protocol analysis, and documentation of undisclosed hardware communication specs.",
            IsDownload: false,
            IsExternal: false,
            GithubUrl: "https://github.com/Mason23Johnson/SteelSeries-Arctis-7-USB-HID-Protocol-Reverse-Engineering")
    };

    private sealed record ProjectItem(
        string Title,
        string Summary,
        string? Thumbnail,
        string? ProjectUrl,
        string Status,
        string Platform,
        string[] Tags,
        string DeepDescription,
        string BuildNotes,
        bool IsDownload = false,
        bool IsExternal = false,
        string? GithubUrl = null);
}
